# CogniVault Medical AI — BiGG Aksiyon Planı

Kaynak: `CogniVaultAI_BiGG_IsPlani_TAM_v2.docx` (TÜBİTAK BiGG 1812 İş Planı, Revize v2)
Proje süresi (resmi): 1 Ekim 2026 – 30 Eylül 2027 (12 ay)
Son güncelleme: 2026-06-19

> **Nasıl kullanıyoruz:** Her gün yeni iş aramıyoruz. Bu dosyadaki sıradaki açık maddeyi alıyoruz, bitirince başına `✅ YAPILDI (tarih)` yazıp bir sonrakine geçiyoruz. Aktif olarak çalıştığımız madde **🔵 ŞU AN** ile işaretli.
>
> Durum etiketleri: `⬜ AÇIK` · `🔵 ŞU AN` · `🟡 DEVAM` · `✅ YAPILDI`

---

## Genel İlerleme Özeti

| İş Paketi | Konu | Resmi Ay | Durum |
|-----------|------|----------|-------|
| İP-1 | Türkçe Diş Triyaj NLU + Kalibre Çekimser Yönlendirici | Ay 1–7 | 🟡 MVP iskeleti var, Ar-Ge açık |
| İP-2 | Deterministik Yönetişim Zarfı'nın Sertleştirilmesi | Ay 1–8 | ✅ Adversarial-kanıtlı (160 senaryo, ihlal=0) |
| İP-3 | On-prem Yerel Yığın Optimizasyonu | Ay 3–9 | 🟡 Gecikme ölçüm/rapor ✅; damıtma (3.6) + RT-ses (3.7) açık |
| İP-4 | Hekim-Döngülü İyileştirme + Öngörücü Süreklilik | Ay 5–10 | 🟡 Shadow Mode var, no-show motoru açık |
| İP-5 | Pilot Saha & Klinik Doğrulama | Ay 7–12 | ⬜ Açık |
| İP-6 | Ticarileştirme & Fikri Mülkiyet | Ay 9–12 | ⬜ Açık |

**🔵 ŞU ANKİ ODAK:** İP-1 ✅ ve İP-2 ✅ tamamlandı. İP-3 gecikme ölçüm+raporlama (3.8/3.9) ✅. İP-4.2 ✅ · İP-4.3 ✅ · İP-4.5 (no-show modeli, TEST AUC 0,83) ✅. Kalan İP-3: 3.6 (model damıtma) ve 3.7 (RT-ses) — gerçek eğitim/ses altyapısı gerekir. İP-4.6 (dinamik slot önerisi) ✅. Sıradaki saf-Python işler: İP-4.4 (karar süresi <30 sn ölçüm) ve İP-4.7 (proaktif geri-çağırma — no-show riskini tüketir).

---

## İP-1 — Türkçe Diş Triyaj NLU + Kalibre Çekimser Yönlendirici
**Başarı ölçütü:** Branş eşlemede ≥%90 · kalibrasyon ECE<0,05 · acil-recall ≈ %100 (kaçan acil ≈ 0) · ≥500 anonim senaryo.
**Çıktı:** Yönlendirici + kalibrasyon raporu. **Sorumlu:** Efe.

- ✅ **1.1** YAPILDI (2026-06-19) Branş + aciliyet ontolojisi tanımlandı — tek doğru kaynak `backend/app/clinical/ontology.py` (10 branş + genel, 3 aciliyet seviyesi). `clinical_ai_service` ontolojiye bağlandı; 40 test geçti (`tests/test_clinical_ontology.py`). Hekim onayına hazır şema.
- ✅ **1.2** YAPILDI (2026-06-19) Türkçe diş şikâyeti korpusu kuruldu — `backend/app/clinical/corpus/`. Deterministik sentetik üretici (`build.py`) → **546 etiketli senaryo** (11 branş + 3 aciliyet + 4 kanal, dengeli, PII-temiz); 35 vakalık elle küratörlü **golden** değerlendirme seti (`build_golden.py`). Etiketler ontolojiye bağlı (yer-doğruluğu). 13 test (`tests/test_corpus.py`), üretici↔dosya senkron kapısı dahil. KVKK: tamamen sentetik/anonim, gerçek hasta verisi yok.
- ✅ **1.3** YAPILDI (2026-06-22) Türkçe argo normalizasyon hattı — `app/clinical/normalizer.py`. 35 argo → kanonik terim kuralı (endodonti, restoratif, periodontoloji, pedodonti, ortodonti, çene cerrahisi, implant, estetik, medikal estetik, dermatoloji, acil). `expand_complaint()` ham metni zenginleştirir; `triage()` sarmalayıcı ham metin → branş + aciliyet tek adımda verir. `tests/test_normalizer.py` — 45 test (genişletme kuralları, uçtan uca branş yönlendirme, aciliyet tespiti, yanlış-pozitif koruması). Tüm mevcut testler regresyonsuz.
- ✅ **1.4** YAPILDI (2026-06-22) Branş yönlendirme modeli — sentetik korpusta **%99.3 doğruluk** (542/546), hedef ≥%90 aşıldı. `match_specialty` skorlama tabanlına yükseltildi (en çok+en uzun eşleşme; normalize tekilleştirme) — "dudak dolgusu"nun "dolgu"ya kapılması gibi alt-dizi çakışmaları çözüldü. Ontoloji anahtar kelimeleri genişletildi (perio: "diş etler"/"kanama"; çene: "çene kırığı"), zamir çakışması yapan dermatoloji "ben" anahtarı kaldırıldı. Normalizer'a kalan argo kuralları eklendi (apse→endodonti, çatladı, kaşınma/mol, termal hassasiyet, çekim varyantları, 20lik genişletme). Yeniden kullanılabilir değerlendirme harness'ı: `app/clinical/evaluate.py` (`python -m app.clinical.evaluate`). Golden set (kasıtlı zor) %62.9 — kalan X→genel_dis kaçışları İP-1.6 çekimser tahminin (insana yükseltme) hedefi, kurala zorla uydurulmadı. Tüm bağımsız testler (ontoloji+korpus+normalizer = 78) geçti.
- ✅ **1.5** YAPILDI (2026-06-23) Güven kalibrasyon katmanı — `app/clinical/calibration.py` (saf Python, numpy/sklearn yok; KVKK local-first). Eşleşme marjından ham güven sinyali + isotonic regresyon (Pool-Adjacent-Violators) ile [0,1] kalibre güven. Sentetik korpus TEST setinde **ECE = 0.0107 < 0,05** (hedef karşılandı; naif normda 0,5421'di). `app/clinical/calibrate.py` rapor+artefakt üreticisi (`python -m app.clinical.calibrate`); kalibratör `data/calibration.json`'a yazılır (JSON, denetlenebilir). `triage()` artık kalibre `confidence` döndürür (kalibratör yoksa 0.0 → güvenli varsayılan, İP-1.6 insana yükseltir). `ontology.rank_specialties()` tek skorlama kaynağı olarak eklendi (match_specialty davranışı korundu). `tests/test_calibration.py` — 13 test. Golden set ECE 0,357 (aşırı-güvenli) → İP-1.6'nın hedefi.
- ✅ **1.6** YAPILDI (2026-06-29) Çekimser/selective prediction katmanı — `app/clinical/selective.py`. `decide()` triyaj sonucunu alıp **kabul (otomatik yönlendir)** mı yoksa **çekimser (insana yükselt)** mi kararı verir. Üç gerekçe: `NO_EVIDENCE` (hiç branş anahtarı yok → genel diş'e düşüş), `AMBIGUOUS_EVIDENCE` (ilk iki branş eşit skor), `LOW_CONFIDENCE` (kalibre güven < eşik). Konformal risk-kontrollü eşik (`fit_threshold`): kabul edilen tahminlerde ampirik hata ≤ %5 olacak en küçük güven eşiği (sentetikte 0.9877). `escalate_to_human` aciliyet yükseltmesiyle birleşir (acil/öncelik OR branş-çekimserliği). **Sonuç:** golden sette ham doğruluk %62,9 → kabul edilenlerde **selektif doğruluk %81,8** (13 vaka insana yükseltildi; 9 sessiz "genel diş'e kaçış" yanlış-yönlendirmesi yakalandı). `select_report.py` risk-kapsam raporu + artefakt üreticisi (`python -m app.clinical.select_report`); eşik `data/selective.json`'a yazılır. `tests/test_selective.py` — 16 test. Tüm bağımsız klinik testler (ontoloji+korpus+normalizer+kalibrasyon+selektif = 107) geçti. Kalan 4 golden hatası yüksek-güvenli karışmalar → İP-1.7 adversarial kapsamının hedefi.
- ✅ **1.7** YAPILDI (2026-06-29) Acil-recall ≈ %100 garantili kapsama — adversarial acil seti + yüksek-recall tespit. `corpus/build_emergency.py` → **40 vakalık adversarial set** (30 sıra dışı/argo/parafraz acil + 10 "acile benzeyen tuzak" hard-negative). Normalizer ACİL kuralları yüksek-recall'a sertleştirildi: kontrolsüz kanama ("kanama bir türlü durmuyor", "kan durmak bilmiyor", "akmaya devam"), havayolu/şişlik ("boğazım şişiyor", "soluk alamıyorum", "nefes daralıyor"), çene/yüz travması ("çenem kırık", "sert cisim çarptı"), bilinç ("bayılacağım", "sersemledim"). **Precision koruması:** ontolojiden bare `"nefes"` anahtarı kaldırıldı (halitozis "nefesim kokuyor" artık acile kaçmıyor); travma kuralları "sinire vurdu" (ağrı idiyomu) ve "dolgu/lamina düştü" (restoratif) tuzaklarını dışlayacak şekilde daraltıldı. **Sonuç:** 3 korpus genelinde (sentetik+golden+adversarial) **76/76 acil tespit = recall %100, kaçan acil = 0, yanlış-pozitif %0** (545 acil-olmayan vakada). `emergency_report.py` recall kapsama panosu (`python -m app.clinical.emergency_report`). `tests/test_emergency.py` — 44 test (parametrik adversarial recall + hard-negative precision + halitozis regresyonu + korpus-genel recall kapısı). Bağımsız klinik suite 151 test geçti.
- ✅ **1.8** YAPILDI (2026-06-30) Konsolide kalibrasyon/metrik panosu — `app/clinical/report.py` (`python -m app.clinical.report`). İP-1.4–1.7'nin dört tekil üreticisini (branş doğruluğu `evaluate.py`, ECE `calibrate.py`, acil-recall `emergency_report.py`, selektif risk `selective.py`) **tek denetlenebilir panoda** toplar; metriği yeniden hesaplamaz, mevcut public fonksiyonları orkestre eder. `build_dashboard()` JSON-hazır sözlük döndürür: her metrik bloğu kendi **hedefini + `pass` bayrağını** taşır, `overall_pass` hepsinin VE'si. `render()` okunabilir konsol panosu; artefakt deterministik (timestamp yok, diff'lenebilir) `data/metrics_report.json`'a yazılır. **Pano sonucu (hepsi ✅):** branş doğruluğu sentetik %96,7 (≥%90) · TEST ECE 0,0209 (<0,05) · acil-recall 76/76 = %100 (kaçan 0) · sentetik selektif risk %0,8 (≤%5). `tests/test_report.py` — 26 test: happy-path (blok bütünlüğü + standalone üreticilerle tutarlılık + dört hedef kapısı + JSON round-trip/determinizm) **artı** negatif/kapı testleri (`overall_pass` saf fonksiyonu — bir blok bile başarısızsa pano başarısız; render ❌), CLI smoke (subprocess `--no-save`/`--json`), sayısal değişmezler (oran∈[0,1], sayım ilişkileri) ve artefakt tazelik kapısı (commit'li `metrics_report.json` ↔ üretici). Bağımsız klinik suite **177 test** geçti (önceki 151 + 26), regresyon yok. **İP-1 tamamlandı** — başarı ölçütleri (branş ≥%90, ECE<0,05, acil-recall ≈%100, ≥500 senaryo) panoda kanıtlanıyor.

## İP-2 — Deterministik Yönetişim Zarfı'nın Sertleştirilmesi
**Başarı ölçütü:** 150+ senaryoda %100 doğru kapı · kapı ihlali = 0 (adversarial) · teşhis/sınır-ötesi/kimlik sızıntısı = 0.
**Çıktı:** Zarf + test koşumu + denetim izi. **Sorumlu:** Enes.

- ✅ **2.1** Veri-sınıfı kapısı (özel-nitelikli sağlık / finansal / kimlik / ses metadata sınıflandırması). — Mevcut: `services/clinical_compliance_service.py`, `agent/policy.py`.
- ✅ **2.2** İnsan-yükseltme tetikleyicileri (acil/sigorta/kimlik/düşük-güven). — Mevcut: `policy.py` GovernanceContext.
- ✅ **2.3** YAPILDI (2026-06-30) Teşhis/tedavi talimatı bloklama — adversarial testle ihlal-edilemezlik kanıtlandı. `no_diagnosis_or_treatment_instruction` kontrolü zarfın HER çıktısında yapısal olarak bulunuyor (ifadeye/duruma bağlı değil); 20 teşhis-talebi senaryosu (parafraz/argo) + 3982 enumeratif değişmez kombinasyonunda teşhis kapısı 0 ihlal.
- ✅ **2.4** YAPILDI (2026-06-30) Sınır-ötesi (residency) modu ve rıza kapıları — formal test edildi. Harici transfer kapalıyken `block_cross_border_ai_processors` kontrolü garanti + sınır-ötesi rıza listelenmiyor; local-first + harici-izin çelişkisi otomatik insana yükseltiliyor. Değişmez testlerinde (3 mod × 2 bayrak) sınır-ötesi sızıntısı 0.
- ✅ **2.5** Maskeli denetim izi (PII maskeleme + audit log). — Mevcut: `services/audit_service.py`, redaction hattı.
- ✅ **2.6** YAPILDI (2026-06-30) Property-based test paketi — `tests/test_governance_invariants.py`. `hypothesis` bağımlılığı eklemeden girdi uzayını deterministik tarar (her ClinicIntent × 3 ikamet modu × harici-izin bayrağı × 10 metin) ve zarfın HER çıktısında geçerli 6 yapısal değişmezi doğrular: teşhis kapısı her zaman var, hekim-onay kontrolü her zaman var, `auto_send ⟺ inceleme-gerekçesi-yok`, maskeli önizleme PII sızdırmaz, sınır-ötesi engel/rıza tutarlı, contact_data hep sınıflı. **3982 kombinasyon geçti.** Saf import, `--noconftest` ile koşar (mevcut `test_hardening.py` HTTP client fixture'ına bağlıydı; bu katman fonksiyon seviyesinde).
- ✅ **2.7** YAPILDI (2026-06-30) Adversarial test paketi — `app/governance/adversarial.py` (deterministik **160 senaryoluk** korpus + ihlal değerlendirici) + `tests/test_governance_adversarial.py`. Kategoriler: PII-maskeleme kaçınması (96), teşhis-talebi (20), acil-yükseltme (12), sağlık-recall (12), sigorta (8), kimlik (6), sınır-ötesi (6). **160/160 senaryoda kapı-ihlali = 0.** Adversarial kapsam gerçek bir KVKK kimlik-sızıntısı buldu ve kapattı: noktayla yazılmış kimlik (`123.456.789.01`) maskelemeden kaçıyordu → `mask_identifiers` telefon/kimlik deseni `.` ayırıcısını kapsayacak şekilde sertleştirildi (kısa sürüm-no/tarih/para/saat aşırı-redakte edilmiyor). Sızıntı regresyonu testlerle kilitlendi.
- ✅ **2.8** YAPILDI (2026-06-30) Kapı ihlal-edilemezliği kanıt raporu — `app/governance/gate_report.py` (`python -m app.governance.gate_report`) + `tests/test_gate_report.py`. Adversarial korpusu koşturup denetlenebilir kanıt panosu üretir: senaryo sayısı (160 ≥150), kapı-ihlali (0), kategori kırılımı, **üç sızıntı kapısı** (teşhis/sınır-ötesi/kimlik = 0 ihlal) ve **denetim izi örnekleri** (maskeli önizleme + kapı kararı). Deterministik artefakt `data/gate_report.json` (timestamp yok, diff'lenebilir). **İP-2 başarı ölçütü kanıtlandı: 150+ senaryoda kapı-ihlali = 0, üç sızıntı = 0.** Bağımsız governance suite 4165 test; klinik+governance toplam **4342 test** geçti.

## İP-3 — On-prem Yerel Yığın Optimizasyonu
**Başarı ölçütü:** On-prem yanıt gecikmesi hedefi karşılanır · modest donanımda klinik-yönlendirme kalitesi.
**Çıktı:** Optimize yerel yığın + gecikme raporu. **Sorumlu:** Efe.

- ✅ **3.1** Yerel ASR (faster-whisper) entegrasyonu. — Mevcut: commit `890102d`, `ai/voice_factory.py`.
- ✅ **3.2** Yerel TTS (Piper tr_TR) entegrasyonu. — Mevcut: commit `890102d`.
- ✅ **3.3** Yerel LLM (Qwen2.5 sınıfı / Ollama) entegrasyonu. — Mevcut: commit `ad6d49b`, `ai/ai_factory.py`.
- ✅ **3.4** PII maskeleme hattı (her işleyiciden önce). — Mevcut: compliance service.
- ✅ **3.5** Harici LLM/STT/TTS klinik veride varsayılan kapalı. — Mevcut: residency varsayılanları.
- ⬜ **3.6** Küçük on-prem modelin TR diş korpusunda damıtma/ince-ayarı (İP-1.2 korpusuna bağlı).
- ⬜ **3.7** Gerçek-zamanlı ses: VAD, gürültü toleransı, kesinti yönetimi.
- ✅ **3.8** Gecikme optimizasyonu — modest donanımda hedef latency ölç ve raporla. — YAPILDI (2026-06-30): `app/perf/latency.py`. Her istekte koşan saf-Python kritik yolu (PII maskeleme→zarf→niyet→triyaj) gerçek `perf_counter` ile p50/p95/p99 ölçer, modest-donanım p95 bütçesine göre kapı uygular. Saf kritik yol p95 ~0,8 ms; model aşamaları (ASR≤1500/LLM≤2500/TTS≤600 ms) belgeli hedef; uçtan-uca ses-turu p95 ~4,6 s (bütçe ≤5 s) ✅. `python -m app.perf.latency --latency-only`.
- ✅ **3.9** Gecikme + kalite raporu. — YAPILDI (2026-06-30): `build_perf_report()` gecikme panosunu (İP-3.8) klinik kalite panosuyla (İP-1.8: branş %96,7 / ECE 0,0209 / recall %100 / seçici risk %0,8) tek denetlenebilir JSON'da birleştirir → `app/perf/data/latency_report.json`. `python -m app.perf.latency`.

## İP-4 — Hekim-Döngülü İyileştirme + Öngörücü Süreklilik
**Başarı ölçütü:** Onay başına karar <30 sn · no-show AUC≥0,75 · pilotta iptal/kaçan-çağrı kurtarımı ≥%60.
**Çıktı:** RLHF döngüsü + süreklilik servisi. **Sorumlu:** Enes.

- ✅ **4.1** Shadow Mode hekim onay akışı. — Mevcut: clinic admin panel + decision log (`DecisionLogView.tsx`, `test_phase3_decisions.py`).
- ✅ **4.2** YAPILDI (2026-06-30) Hekim onay/düzeltmelerinden RLHF etiket üretimi — yeni `app/learning/labels.py`. Shadow Mode'da hekim kararını (`ShadowReview` → `ClinicalModelFeedback`, `training_status="pending_redaction"`) **mahremiyet-güvenli etiket setine** çevirir. İki değişmez: (1) **mahremiyet kapısı** — `pending_redaction` (redaksiyon onayı verilmemiş) kayıtlar etikete ASLA girmez, ham hasta metni hiç taşınmaz (yalnızca review_id + niyet + karar); (2) **determinizm** — sıralı, timestamp'siz artefakt. `outcome→label`: onay→`positive_confirm`, düzeltme→`correction`, ret→`negative`. `_partition` tek dedup kaynağı (review_id ile tekil, son kayıt kazanır) → değişmez **`trainable == etiket sayısı`**. `build_report` kapıları: mahremiyet-sızıntısı=0, bütünlük (geçerli tip + ağırlık∈[0,1] + tekil id). Opsiyonel DB adaptörü (`feedback_records_from_db`, import yalnızca fonksiyon içinde) gerçek döngüyü besler; çekirdek DB'siz test edilir. Artefakt `data/labels.json`; CLI `python -m app.learning.labels`. `tests/test_learning_labels.py` — 12 test (mahremiyet kapısı, eşleme, dedup, istatistik tutarlılığı, kapılar, determinizm, artefakt tazeliği). Saf-import, `--noconftest`. Odak İP-4.3/4.5'e taşındı.
- ✅ **4.3** YAPILDI (2026-06-30) Onaylardan otomatik-yanıt eşiği öğrenme (kapı kurallarını İHLAL ETMEDEN) — yeni `app/learning/thresholds.py`. İP-4.2 `FeedbackRecord`'unu kullanır: hekim düzeltmesiz onayladıysa o güvende otomatik yanıt doğru olurdu; düzeltti/reddettiyse hata. Konformal benzeri uydurma (bkz. `selective.py`): hata bütçesini (≤%5) karşılayan **en küçük** eşik seçilir → otomasyon kapsamı azami, güvenlik korunur. **Üç yumuşatılamaz kapı:** (1) acil/sigorta (`FORCE_HUMAN_INTENTS`) otomatik havuza HİÇ girmez; (2) önerilen eşik `SAFETY_FLOOR=0.75` (mevcut shadow eşiği) altına inemez; (3) otomatik-yanıt hata ≤ bütçe. Bütçe hiçbir eşikte tutmazsa otomasyon güvenli şekilde KAPANIR (NO_AUTO_REPLY). Sentetik onay setinde öğrenilen eşik **0,90** (kapsam %33, hata %0). `build_report` kapıları + deterministik artefakt `data/thresholds.json` + CLI `python -m app.learning.thresholds`. `tests/test_learning_thresholds.py` — 9 test (acil hariç, taban, en-küçük-eşik, bütçe-imkânsız→kapalı, kapılar, determinizm, artefakt tazeliği). Saf-import `--noconftest`. Odak İP-4.5 (no-show modeli)'ne taşındı.
- ⬜ **4.4** Onay başına karar süresi <30 sn ölçüm + optimizasyon.
- ✅ **4.5** YAPILDI (2026-06-30) No-show / süreklilik risk modeli — yeni `app/learning/noshow.py`. Saf-Python lojistik regresyon (standardize + gradient descent; numpy/sklearn yok). Randevu/hasta özelliklerinden (lead time, geçmiş gelmeme/tamamlanan, hatırlatma, ilk-ziyaret, son ziyaretten beri gün, yaş, saat) gelmeme olasılığı üretir. Rank-temelli AUC (Mann-Whitney U, bağ-toleranslı). Deterministik sentetik set (1600 kayıt, no-show ~%29) + 70/30 split. **Sonuç: TEST AUC = 0,8301 ≥ 0,75 (hedef karşılandı)**; öğrenilen yönler doğru (prior_no_shows +, prior_completed −, reminder −). `NoShowModel.risk_score()` ham özellikten [0,1] risk verir — İP-4.6/4.7 bunu tüketecek. `build_report` AUC kapısı + deterministik artefakt `data/noshow.json` + CLI `python -m app.learning.noshow`. `tests/test_learning_noshow.py` — 12 test (AUC yardımcı doğruluğu, kabul ölçütü, katsayı yönleri, risk monotonluğu, determinizm, artefakt tazeliği). Saf-import `--noconftest`. Odak İP-4.4/4.6/4.7'ye taşındı.
- ✅ **4.6** YAPILDI (2026-06-30) Dinamik slot önerisi motoru — yeni `app/learning/slots.py`. İP-4.5 no-show riskini tüketir: bir hastaya hangi slotların önerileceğini doluluk dengesi + gelmeme riskiyle birlikte sıralar. **Riske duyarlı sezgi:** riskli hastada (risk ≥ eşik) yoğun/prime saatler cezalandırılır (gelmezse değerli kapasite boşa gitmesin), hatırlatma penceresi olan slotlar ödüllendirilir (İP-4.7 köprüsü); güvenilir hastada prime tercih edilebilir. Dolu slot (kapasite ≤ 0) asla önerilmez; sıralama deterministik (skor↓, gün/saat/id). `recommend_for_patient(model, ...)` İP-4.5 modeliyle gerçek entegrasyon. `build_report` 4 kapı (dolu-önerilmez · riskli-hatırlatma-öncelikli · riskli-yoğun-#1-değil · sıralı-sınırlı) + deterministik artefakt `data/slots.json` + CLI `python -m app.learning.slots`. `tests/test_learning_slots.py` — 12 test. Saf-import `--noconftest`. Odak İP-4.4/4.7'ye taşındı.
- ⬜ **4.7** Kişiye özel proaktif geri-çağırma zamanlaması (yarım kalan tedavi, geciken kontrol).

## İP-5 — Pilot Saha & Klinik Doğrulama
**Başarı ölçütü:** ≥%85 memnuniyet · ≥3 referans · saha kalibrasyon raporu · sıfır güvenlik-kapısı ihlali.
**Çıktı:** Metrik panosu + saha raporu. **Sorumlu:** Enes+Efe.

- ⬜ **5.1** 3–5 pilot klinik bul ve sözleşme/rıza süreçlerini hazırla.
- ⬜ **5.2** Canlı pilot dağıtımı (on-prem kurulum + onboarding).
- ⬜ **5.3** Saha kalibrasyon ve güvenlik kapısı doğrulaması.
- ⬜ **5.4** Kaçan çağrı kurtarma / dönüşüm / memnuniyet metrik panosu.
- ⬜ **5.5** Haftalık iterasyon döngüsü.
- ⬜ **5.6** Saha raporu + ≥3 referans toplama.

## İP-6 — Ticarileştirme & Fikri Mülkiyet
**Başarı ölçütü:** ≥5 ücretli klinik · vekil-incelemeli patent başvuru paketi (3 bağımsız istem) · onboarding <1 gün.
**Çıktı:** Faturalama + patent dosyası + Faz-2 notu. **Sorumlu:** Efe+Enes.

- 🟡 **6.1** Abonelik + başarı-bazlı faturalama (Starter/Growth/Enterprise). — Mevcut: `services/billing_service.py` temeli var; tam fiyat modeli açık.
- 🟡 **6.2** Çoklu-kiracı (multi-tenant) dağıtım. — Mevcut: `test_phase1_tenant.py` tenant izolasyonu var; onboarding <1 gün hedefi açık.
- ⬜ **6.3** Onboarding akışını <1 güne indir.
- ⬜ **6.4** Yöntem patenti novelty araştırması (prior-art, TÜRKPATENT EPATS).
- ⬜ **6.5** Patent istem taslakları — 3 bağımsız istem (yönetişim zarfı / kalibre triyaj / hekim onay paketi) + bağımlı istemler.
- ⬜ **6.6** Vekil-incelemeli patent başvuru paketi.
- ⬜ **6.7** Federe öğrenme Faz-2 fizibilite notu.
- ⬜ **6.8** ≥5 ücretli klinik dönüşümü.

---

## Kilometre Taşları (resmi takvim)
- **Ay 6:** Kalibre çekimser yönlendirici (ECE<0,05) MVP — İP-1 çıktısı.
- **Ay 8:** Adversarial-test ile ihlal-edilemezliği kanıtlanmış yönetişim zarfı — İP-2 çıktısı.
- **Ay 10:** Hekim-döngülü iyileştirme + öngörücü süreklilik pilotta — İP-4 çıktısı.
- **Ay 12:** ≥5 ücretli klinik + vekil-incelemeli patent paketi (3 bağımsız istem) — İP-6 çıktısı.

## Değişiklik Günlüğü
- 2026-06-19: Plan oluşturuldu. Mevcut MVP üzerinden İP-2 (zarf), İP-3 (yerel yığın), İP-4 (shadow mode) temel maddeleri YAPILDI işaretlendi. Aktif odak İP-1.1.
- 2026-06-19: İP-1.1 YAPILDI. `app/clinical/ontology.py` ontoloji modülü eklendi (branş + aciliyet, tek kaynak); `clinical_ai_service` yinelenen tanımlardan arındırılıp ontolojiye bağlandı; `tests/test_clinical_ontology.py` (12 test) eklendi. Odak İP-1.2'ye taşındı.
- 2026-06-19: İP-1.2 YAPILDI. `app/clinical/corpus/` paketi eklendi: schema (CorpusEntry + JSONL + PII tarama), şablonlar, deterministik üretici (546 senaryo) ve 35 vakalık golden set. `tests/test_corpus.py` (13 test). Tüm paket 168 test geçti, regresyon yok. Odak İP-1.3'e taşındı.
- 2026-06-22: İP-1.3 YAPILDI. `app/clinical/normalizer.py` eklendi: 35 kural argo → kanonik terim genişletme motoru + `triage()` sarmalayıcı. `tests/test_normalizer.py` (45 test). Odak İP-1.4'e taşındı.
- 2026-06-22: İP-1.4 YAPILDI. Skorlama tabanlı `match_specialty` + genişletilmiş normalizer/ontoloji → sentetik korpusta %74→%99.3. `app/clinical/evaluate.py` değerlendirme harness'ı eklendi. NOT: çalışma ağacındaki `app/models/entities.py` bozuk (eksik import'lar + kayıp `OutreachDraft` sınıfı) — İP-1.4 dışı, ayrı blocker; servis tarafı testleri (`test_clinical.py`) bu yüzden koşmuyor. Odak İP-1.5'e taşındı.
- 2026-06-23: `entities.py` blocker'ı çözüldü (commit `c368d31`); `test_clinical.py` tekrar koşuyor. Alembic çift-head bug'ı (merge yan etkisi) `0009_merge_heads` ile giderildi. Reverted `codex/clinical-receptionist-foundation` branch'inden 21 faydalı test + 3 doküman kurtarıldı; kurtarılan test `voice.py`'de gerçek bir `NameError` (500) bug'ı yakaladı, düzeltildi.
- 2026-06-23: İP-1.5 YAPILDI. Saf-Python isotonic kalibrasyon (`calibration.py`) + rapor/artefakt üreticisi (`calibrate.py`). Sentetik TEST ECE **0.0107 < 0,05**. `triage()` kalibre `confidence` alanı kazandı (İP-1.6 köprüsü). `ontology.rank_specialties()` eklendi. `tests/test_calibration.py` (13 test). Backend 526 passed, 1 skipped. Odak İP-1.6'ya taşındı.
- 2026-06-29: İP-1.6 YAPILDI. Çekimser tahmin katmanı (`selective.py`): kanıtsız/çelişkili/düşük-güvenli triyajı otomatik insana yükseltir; konformal risk-kontrollü eşik (`fit_threshold`, kabul hatası ≤ %5). Golden selektif doğruluk %62,9 → **%81,8** (9 sessiz genel-diş kaçışı yakalandı). `select_report.py` + `data/selective.json` artefaktı. `tests/test_selective.py` (16 test); bağımsız klinik suite 107 test geçti. NOT: yerel `conftest.py` `app.main` import'unda takılıyor (ortam/ağ kaynaklı, İP-1.6 dışı); klinik testler `--noconftest` ile koşturuldu. Odak İP-1.7'ye taşındı.
- 2026-06-29: İP-1.7 YAPILDI. Adversarial acil seti (`corpus/build_emergency.py`, 30 acil + 10 hard-negative) + normalizer ACİL kurallarının yüksek-recall'a sertleştirilmesi. Ontolojiden bare `"nefes"` kaldırıldı (halitozis yanlış-pozitifi giderildi); travma kuralları idiyom/restoratif tuzaklarına karşı daraltıldı. **Sonuç: 3 korpus genelinde 76/76 acil = recall %100, kaçan acil 0, yanlış-pozitif %0.** `emergency_report.py` + `tests/test_emergency.py` (44 test). Bağımsız klinik suite 151 test geçti. NOT: `clinical_ai_service.extract_clinical_intake` hâlâ kendi sabit-kodlu acil override'larını kullanıyor (normalizer'dan bağımsız) — gelecekte tek motora birleştirme adayı. Odak İP-1.8'e taşındı.
- 2026-06-30: İP-1.8 YAPILDI. Konsolide metrik panosu (`report.py`): branş doğruluğu + ECE + acil-recall + selektif risk tek denetlenebilir JSON panoda (`data/metrics_report.json`), her metrik hedef+pass bayrağıyla, `overall_pass` VE'si. Tekil üreticileri orkestre eder (yeniden hesaplama yok). Pano: branş %96,7 · ECE 0,0209 · recall %100 · selektif risk %0,8 — hepsi ✅. `tests/test_report.py` (26 test: happy-path + negatif/kapı + CLI smoke + invariant + artefakt tazelik); bağımsız klinik suite 177 test geçti. **İP-1 tamamlandı.** Odak İP-2'ye taşındı.
- 2026-06-30: İP-2.3/2.4/2.6/2.7/2.8 YAPILDI — **İP-2 tamamlandı.** Yönetişim Zarfı adversarial sertleştirme: yeni `app/governance/` paketi (`adversarial.py` 160-senaryo korpus + ihlal değerlendirici, `gate_report.py` kanıt raporu + `data/gate_report.json`). Property-based değişmez paketi (`tests/test_governance_invariants.py`, 3982 kombinasyon) + adversarial paket (`tests/test_governance_adversarial.py`) + rapor testi (`tests/test_gate_report.py`). **160/160 senaryoda kapı-ihlali = 0; teşhis/sınır-ötesi/kimlik sızıntısı = 0.** Adversarial kapsam gerçek bir kimlik-sızıntısı buldu (`123.456.789.01` noktalı TC) → `mask_identifiers` sertleştirildi. Bağımsız governance 4165 test; klinik+governance toplam 4342 test geçti. NOT: governance testleri de saf-import, `--noconftest` ile koşar; mevcut `test_hardening.py` HTTP `client` fixture'ına bağlı olduğu için ayrı tutuldu. Odak İP-3/İP-4'e taşındı.
- 2026-06-30: İP-3.8/3.9 YAPILDI. Yeni `app/perf/` paketi (`latency.py`): yerel yığının her istekte koşan saf-Python kritik yolunu (PII maskeleme→yönetişim zarfı→niyet sınıflama→klinik triyaj) gerçek `perf_counter` ile benchmark'lar, p50/p95/p99 çıkarır ve modest-donanım p95 bütçesine göre kapı uygular. **Ölçüm: saf kritik yol p95 ~0,8 ms** (PII 0,005 / zarf 0,007 / niyet 0,67 / triyaj 0,12 ms — hepsi bütçe içinde). Model aşamaları (ASR≤1500 / LLM≤2500 / TTS≤600 ms) bu ortamda ölçülemez → belgeli hedef; **uçtan-uca ses-turu p95 tahmini ~4,6 s (bütçe ≤5 s) ✅**. İP-3.9: `build_perf_report()` gecikmeyi İP-1.8 kalite panosuyla birleştirir → `app/perf/data/latency_report.json`. `tests/test_latency_report.py` (17 test: yüzdelik aradeğer + bütçe kapısı + yapı + determinizm + render-fail + birleşik kalite). Bağımsız perf+klinik+governance 4208 test geçti. NOT: gecikme rakamları duvar-saati → artefakt donmuş değil; testler süreyi değil mantık/yapıyı kilitler. Kalan İP-3 (3.6 model damıtma, 3.7 RT-ses) gerçek eğitim/ses altyapısı gerektirir. Odak İP-3.6/3.7 veya İP-4'e taşındı.
- 2026-06-30: İP-4.6 YAPILDI. Dinamik slot önerisi motoru — `app/learning/slots.py`. No-show riskine duyarlı slot sıralama: riskli hastada prime cezası + hatırlatma ödülü, dolu slot elenir, deterministik. `recommend_for_patient` İP-4.5 modeliyle entegre. 4 kapı + artefakt `data/slots.json`. `tests/test_learning_slots.py` (12 test) geçti. Odak İP-4.4/4.7'ye taşındı.
- 2026-06-30: İP-4.5 YAPILDI. No-show risk modeli — `app/learning/noshow.py` (saf-Python lojistik regresyon + rank-AUC). Sentetik 1600 randevu (no-show ~%29), TEST **AUC 0,8301 ≥ 0,75** ✅. `NoShowModel.risk_score()` İP-4.6/4.7 için risk API'si. `tests/test_learning_noshow.py` (12 test) geçti. Odak İP-4.4/4.6/4.7'ye taşındı.
- 2026-06-30: İP-4.3 YAPILDI. Onaylardan otomatik-yanıt eşiği öğrenme — `app/learning/thresholds.py`. İP-4.2 sinyalinden konformal benzeri eşik uydurma; üç yumuşatılamaz kapı (acil daima insana · taban 0,75 altına inilmez · hata ≤%5). Sentetikte öğrenilen eşik 0,90 (kapsam %33, hata %0); bütçe tutmazsa otomasyon güvenli kapanır. `tests/test_learning_thresholds.py` (9 test) geçti. Odak İP-4.5'e taşındı.
- 2026-06-30: İP-4.2 YAPILDI. Hekim-döngülü RLHF veri toplama döngüsü — yeni `app/learning/` paketi (`labels.py`). Shadow Mode hekim kararlarını (`ClinicalModelFeedback`) mahremiyet-güvenli etiket setine çevirir; **`pending_redaction` kayıtlar etikete asla girmez**, ham hasta metni taşınmaz. `outcome→label` (onay/düzeltme/ret → positive_confirm/correction/negative), tek dedup kaynağı (`_partition`) ile `trainable == etiket sayısı` değişmezi. `build_report` kapıları (mahremiyet-sızıntısı=0, bütünlük) + deterministik artefakt `data/labels.json` + CLI `python -m app.learning.labels`. Opsiyonel DB adaptörü gerçek döngüyü besler (import fonksiyon içinde, çekirdek DB'siz). `tests/test_learning_labels.py` (12 test, saf-import `--noconftest`) geçti. Odak İP-4.3 (onaylardan eşik öğrenme) / İP-4.5 (no-show modeli)'ne taşındı.
